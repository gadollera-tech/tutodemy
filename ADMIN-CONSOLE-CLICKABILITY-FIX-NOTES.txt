TutoDemy Admin Console Clickability Fix

Fixed:
- Admin tabs bind immediately instead of waiting for all database requests.
- Header/footer Admin links switch sections reliably without a page reload.
- Overview cards are clickable through delegated events.
- Query-string tabs and browser Back/Forward remain synchronized.
- One failed admin data request no longer disables the whole console.
- Admin JavaScript and CSS use new cache-busting URLs.
- PWA service-worker cache version was advanced to remove stale admin assets.
- Mobile admin tabs have larger, touch-friendly targets.

No Supabase SQL changes are required.
